# Gemini API Key (required)
# Get your API key from: https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key_here

# Redis Cloud connection (optional - falls back to memory cache if not provided)
# Format: redis://[username]:[password]@host:port
# Example: redis://default:your_password@redis-12345.c1.us-west-2-2.ec2.cloud.redislabs.com:12345
REDIS_URL=

# Server settings
HOST=0.0.0.0
PORT=8000

# CORS settings (comma-separated list of allowed origins)
# Use "*" for development, specify exact origins for production
CORS_ORIGINS=*

# Temporary file storage
TEMP_DIR=/tmp/recipe-keeper

# Video processing settings
MAX_VIDEO_SIZE_MB=100
VIDEO_DOWNLOAD_TIMEOUT=60

# Progress message settings (for long-running video processing)
# Show progress message after X seconds
PROGRESS_MESSAGE_DELAY_SEC=10
# Custom message to display
PROGRESS_MESSAGE_TEXT=Still working on it... Video processing can take up to 30 seconds.

# Cache settings
CACHE_ENABLED=true
CACHE_TTL_SECONDS=86400
CACHE_MAX_ITEMS=1000
